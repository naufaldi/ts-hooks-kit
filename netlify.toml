[build]
  base = "apps/docs"
  command = "cd ../.. && corepack enable && pnpm install --frozen-lockfile && pnpm --filter @ts-hooks-kit/core build && pnpm build:docs"
  publish = "build/client"

[build.environment]
  NODE_VERSION = "20.19.0"
  COREPACK_ENABLE_AUTO_PIN = "0"
